const myArr = [10, 20, 30, 40, 50, 60];

//Returns true if the element is found.
console.log(myArr.includes(20));

//Returns true if the element is found after the specified index.
console.log(myArr.includes(20, 1));